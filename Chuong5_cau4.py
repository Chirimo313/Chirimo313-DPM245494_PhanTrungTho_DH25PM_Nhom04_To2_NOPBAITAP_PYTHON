"""
Yêu cầu: 
Trình bày một số hàm quan trọng trong xử lý Chuỗi của Python
| Hàm                   | Chức năng                          |
| --------------------- | ---------------------------------- |
| `s.lower()`           | Chuyển toàn bộ về chữ thường       |
| `s.upper()`           | Chuyển toàn bộ về chữ hoa          |
| `s.title()`           | Viết hoa chữ cái đầu mỗi từ        |
| `s.strip()`           | Xóa khoảng trắng đầu & cuối chuỗi  |
| `s.split(sep)`        | Tách chuỗi theo ký tự phân cách    |
| `'sep'.join(list)`    | Nối danh sách thành chuỗi          |
| `s.replace(old, new)` | Thay thế ký tự                     |
| `s.find(sub)`         | Trả về vị trí con trong chuỗi      |
| `s.count(sub)`        | Đếm số lần xuất hiện của chuỗi con |
| `s.isdigit()`         | Kiểm tra toàn bộ là chữ số         |
| `s.isalpha()`         | Kiểm tra toàn bộ là chữ cái        |

"""
